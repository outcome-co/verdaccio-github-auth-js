{
  "name": "@outcome-co/verdaccio-github-auth",
  "version": "1.2.1",
  "description": "Verdaccio Github Authentication Plugin",
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "lint": "eslint --ignore-path .gitignore --max-warnings 0 .",
    "test": "yarn test:unit && yarn test:integration",
    "test:unit": "TEST_ENV=test jest ./src",
    "test:integration": "TEST_ENV=integration jest ./src -i",
    "pretest": "yarn type-check",
    "type-check": "tsc",
    "clean": "rm -rf dist coverage",
    "build": "yarn clean && babel src -d dist",
    "prepublishOnly": "yarn build",
    "watch": "watch 'yarn build' src --wait=0.5",
    "server": "yarn build && nodemon --delay 1 --watch dist --signal SIGTERM ./run/server.js",
    "secrets:sync": "node ./bin/secrets.js sync",
    "secrets:clear": "node ./bin/secrets.js clear"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/outcome-co/verdaccio-github-auth-js"
  },
  "author": "Engineering <engineering@outcome.co>",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/outcome-co/verdaccio-github-auth-js/issues"
  },
  "homepage": "https://github.com/outcome-co/verdaccio-github-auth-js#readme",
  "dependencies": {
    "@octokit/graphql": "^4.5.4",
    "@verdaccio/commons-api": "^9.7.1",
    "json-to-graphql-query": "^2.0.0",
    "limiter": "^1.1.5",
    "lodash": "^4.17.20",
    "node-cache": "^5.1.2"
  },
  "devDependencies": {
    "@octokit/rest": "^18.0.3",
    "@outcome-co/devkit": "^7.1.0",
    "@verdaccio/types": "^9.7.2",
    "dotenv": "^8.2.0",
    "jest-retries": "^1.0.1",
    "mustache": "^4.0.1",
    "nodemon": "^2.0.4",
    "objectmodel": "^4.2.2",
    "promise-retry": "^2.0.1",
    "tweetsodium": "0.0.5",
    "verdaccio": "^4.8.1",
    "yargs": "^16.0.3"
  },
  "engines": {
    "node": ">=10"
  },
  "optionalDependencies": {
    "@outcome-co/verdaccio-google-cloud": "^1.1.0"
  },
  "resolutions": {
    "node-forge": "^0.10.0"
  }
}
